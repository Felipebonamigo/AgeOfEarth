# Age of Earth — guia para agentes

RTS (Rise of Nations + Age of Mythology, panteão grego) em TypeScript + PixiJS, empacotável com Electron para a Steam.
Idioma da interface e dos comentários: português (Brasil). Código em inglês.

## Comandos
- `npm run dev` — Vite em http://localhost:5173 · `npm run build` — typecheck + bundle em `dist/` · `npm run preview` — serve `dist/` na porta 4173
- `npm test` — vitest · `npm run typecheck`
- `npm run smoke 20 42` — IA x IA por 20 min de jogo (semente 42) sem interface · `npm run balance 35 1,2,3`
- `npx tsx scripts/missions.ts` — valida as missões da campanha
- `npm run map:export saida.map.json --size small --seed 42` gera um mapa fixo; `npm run map:check [arquivo.map.json]` valida (sem arquivo: os embutidos), mostra recursos por início e roda 2 min de IA; `npm run smoke 5 42 -- --map arquivo.map.json` · `npx tsx scripts/horde.ts` — valida o Modo Horda
- Mapas oficiais: `npx tsx scripts/maps/estreito.ts` / `egeu.ts` regeram `src/core/data/maps/<id>.map.json` com as ops do editor (simetria, recursos por início e rotas não seláveis conferidos; `tests/data.test.ts` exige que o arquivo seja exatamente o gerado); `npx tsx scripts/maps/fairness.ts <mapa|id> 45 1-16 zeus --both --jobs 3` mede vitórias/idades por início com o mesmo deus em todos, nas duas ordens de inícios (critério: nenhum lado com > 65 % das decididas + à frente no fim, por posição e por índice; `--mirror-ai` iguala a personalidade das IAs, `--transpose` joga o mapa transposto; com só 16 sementes um "fora" isolado pede confirmação em 32). Desde 26/09/2026 a posição não decide mais (união de 96 partidas: Egeu 45 × 44, Estreito 48 × 46), mas o Estreito 1–16 dá 66 % por posição e o Egeu 1–16 dá 67 % das decididas por índice (as sementes 101–132 não repetem; resíduo de índice em observação, `docs/EDITOR.md`)
- `npm run relay` — servidor de multiplayer (porta 8787)
- `npx tsx scripts/steam-achievements.ts [--check]` — planilha de cadastro das conquistas no Steamworks (`desktop/steam/achievements.{json,csv}`: API name = id, nome/descrição brazilian/english, oculta); `npx tsx scripts/licenses.ts [--check]` — licenças de terceiros dos lockfiles + `node_modules` → `docs/THIRD_PARTY.md` e `src/ui/third-party.json` (tela Créditos); os dois falham com id inválido/tradução faltando e com GPL/AGPL/licença desconhecida no pacote (mesmos critérios de `tests/steam.test.ts`)
- `npm run loadtest -- --minutes 40 --bots 4 [--jitter 0-80] [--drop-at 8 --drop-mode wait|resume|resume-rejoin] [--spectator-at 12] [--map arquivo]` — teste de carga do multiplayer com bots em Node (hashes, banda, reconexão); relatório em `docs/perf/`
- `node scripts/playtest.mjs` / `playtest-campaign.mjs` / `playtest-mp.mjs` / `playtest-reconnect.mjs` / `playtest-options.mjs` / `playtest-i18n.mjs` / `playtest-modes.mjs` / `playtest-horde-replay.mjs` / `playtest-fixedmap.mjs` / `playtest-editor.mjs` / `playtest-mp-fixedmap.mjs` / `playtest-rooms.mjs` / `playtest-spectate.mjs` / `playtest-scenario.mjs [url] [relay]` / `playtest-credits.mjs` (tela Créditos PT/EN → `docs/art/creditos-{pt,en}.png`) / `xvfb-run -a node scripts/playtest-desktop.mjs` (build Electron de `desktop/`, após `npm ci && npm run dist:linux` lá; perfil temporário; inclui o Steam Cloud: apaga o localStorage de `app://game` e confere a restauração dos arquivos) / `playtest-audio.mjs` / `playtest-gamepad.mjs` / `actionshot.mjs` — Chromium headless (Playwright); exigem `npm run preview` e, para o multiplayer, `npm run relay`
- `npm run art:shot` / `npm run art:diff` — capturas de referência (3 zooms, editor, cidade, batalha) em `docs/art/` e diff contra `docs/art/ref/` (2 % de tolerância); `?perf=1` na URL mostra fps/ms/draw calls/MB
- `node scripts/renderperf.mjs http://localhost:4173/ 20 [--quality low|medium|high] [--measure 12000] [--baked off]` — custo do renderizador (ms/quadro em vários zooms) numa partida grande após 20 min simulados; rode antes/depois de mudanças visuais (números pessimistas: renderização por software)
- `node scripts/rendercpu.mjs http://localhost:4173/ [--modes papa] [--label x]` — CPU por quadro do renderizador (nosso código + Pixi) sem o ruído da rasterização por software, arte assada ligada × desligada; JSON em `docs/perf/`
- `node scripts/artparade.mjs http://localhost:4173/ [--prefix etapa4]` — desfile da arte assada em 5 cenas: cena (hoplitas nas 8 direções, luta e aglomerado, cidadãos cortando/carregando/construindo, templo em obra e completo, bosque), roda (as 17 unidades da Etapa 4 nas 8 direções, cavalaria galopando), batalha mista (18 × 18 tipos, heróis usando a Q, uma queda de cada tipo) e fila dos 19 tipos diante de quartel, estábulo, oficina, templo e academia, mais o pick de um hoplita inimigo visto pelo vão da helépole (clique direito = ataque): `docs/art/<prefixo>-{cena-z10,cena-z22,roda-z10,batalha-z10,batalha-z22,desfile-z10,desfile-z22,desfile-z22-2x,procedural-z10}.png`; idas, amostras e esperas no tempo de jogo (tick), não em ms; falha se algum tipo sair procedural, se quem anda olhar para fora da velocidade, se algum tipo não andar nas 8 direções, se > 10 % dos golpes apontarem a 90°+ do alvo, se algum tipo não atacar (e os de arco/dardo mirar), se a Q não tocar `ability`, se alguma queda não sair assada ou se o pick pelo alfa errar
- Arte das unidades (Etapa 4, `docs/ART.md` Apêndice E): `node scripts/bake/pose-preview.mjs <manifesto> --anims attack --dirs 0,2,5 --scale 4 --crop 1.6,1.6 --out x.png` (poses ampliadas sem assar) · `npm run art:bake -- --preview art/examples [--only id]` (exemplos dos rigs de cavalo/cerco/dardos só nas folhas de contato, fora dos atlas) · `node scripts/bake/lineup.mjs --ids hoplite,militia,… [--cell 3.4,4.6]` (fila a zoom 1 direto dos atlas: `docs/art/etapa4-lote1-fila.png`; célula maior para o cerco) · `node scripts/artcavalry.mjs http://localhost:4173/ [--out dir]` (lote cavalaria em jogo: falha se algum montado sair procedural, não galopar/atacar, golpear virado para longe do alvo ou cair sem a queda assada) · `node scripts/artlote-distancia.mjs http://localhost:4173/` (lote distância-cerco no jogo: peltastas e arqueiros cretenses atirando/mirando, petróbolos e helépoles bombardeando e rodando; falha se algum sair procedural) · o `art:check` mede a VRAM por escala
- `node scripts/artheroes.mjs http://localhost:4173/ [--out scratch]` — lote heróis da Etapa 4 no jogo (fila parada e andando, as 5 habilidades Q no meio da animação `ability`, preset alto 2×); falha se algum herói sair procedural, se a Q não tocar `ability`, se o arco de Odisseu não atirar/mirar ou se quem anda olhar para fora da velocidade
- `node scripts/artcity.mjs http://localhost:4173/` — cidade assada (Etapa 3: muralha com portões e torres, casas em obra/danificadas com fumaça, templo, escombros), fantasma de construção e ícones no HUD: `docs/art/etapa3-base-*.png`; falha se os edifícios não saírem assados, se a muralha usar < 9 variantes de bitmask, se o portão não abrir ou se faltar fumaça/escombros/fantasma/ícones · `node scripts/bake/compose.mjs [--art dir]` — prévia da cidade direto dos atlas, sem navegador
- `node scripts/artcity-economia.mjs http://localhost:4173/` — vila do lote economia da Etapa 3 (fazendas nas 3 plantações, celeiro, serraria, mina, mercado, academia, cornucópia em obra/prontos/danificados, escombros, fantasma, ícones): `docs/art/etapa3-economia-*.png`; falha se algum sair procedural ou faltar plantação/obra/dano/fumaça/escombros/ícones
- `node scripts/artages.mjs http://localhost:4173/ [--only arcaica,mitica,…]` — uma cidade murada completa por Idade (todos os edifícios da Idade, obras, danos, escombros, portões, torres) e a praça das 3 maravilhas + Portal dos Titãs: `docs/art/etapa3-cidade-{arcaica,classica,heroica,mitica,maravilhas}.png`; falha se algum edifício sair procedural, não couber ou faltar estado/variante pedido
- `node scripts/artmilitary.mjs http://localhost:4173/` — lote militar assado (quartel, estábulo, oficina, fortaleza, portal dos titãs com o vórtice animado, 3 maravilhas; obras, dano, escombros, fantasma, ícones): `docs/art/etapa3-militar-*.png`
- Chromium do Playwright: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` neste ambiente; use `--use-gl=swiftshader --enable-unsafe-swiftshader`

## Regras do núcleo (`src/core`)
- **Determinismo obrigatório**: nada de `Math.random`, `Math.sin/cos/atan2/pow/exp/log/hypot`, `Date.now`. Use `state.rng` e distâncias por `Math.sqrt`. O teste `tests/determinism.test.ts` falha se isso for violado.
- Toda mutação de estado vinda de fora entra por `Command` → `applyCommand` (`src/core/sim/commands.ts`). O renderizador nunca altera o estado.
- Conteúdo é dado: `src/core/data/*.ts` (unidades, edifícios, tecnologias, deuses, idades). Efeitos de tecnologia seguem os tipos em `techs.ts`.
- Estado serializável: `src/core/serialize.ts` (Maps e typed arrays viram arrays). Novos campos precisam de valor padrão no `deserialize`.
- Cenários: `src/core/scenario/campaign.ts` (dados + gatilhos), executados por `runner.ts` dentro do tick.
- **Justiça de posição**: nenhuma regra da IA/motor usa orientação absoluta ("ao sul do CC", espiral que começa pelo norte, desempate pela ordem da varredura ou por (y, x)). Use `centerFrame` (vetor ponto→centro do mapa) ou `towardFrame` (lado de quem chega, com `tie` para o caso alinhado) de `src/core/map/grid.ts`, centros de tile/pegada, desempate pela distância ao centro do mapa e, se ainda empatar, `frameCompare` no referencial local (inícios na diagonal do mapa, como no Estreito); nada de "quem pensa primeiro" fixo (`aiThinkOrder` gira por rodada, `state.aiRound`). Testes de invariância e as sondagens de simetria do Egeu (30/30) e do Estreito (10/10) em `tests/position-fairness.test.ts`. Mudança que altera a partida de uma mesma semente sobe `SIM_VERSION` (`constants.ts`; o relay recusa clientes de outra versão).

## Camadas
`src/core` (simulação) · `src/render` (PixiJS: texturas procedurais, chunks, névoa, efeitos) · `src/ui` (HUD/menus/entrada em DOM) · `src/game/session.ts` · `src/net` + `server/relay.mjs` (multiplayer lockstep) · `desktop/` (Electron/Steam) · `docs/`.

## Convenções
- Atalhos de construção/treino são únicos por contexto e nunca usam A (atacar-mover), R (ponto de encontro) ou U (liberar); teste em `tests/data.test.ts`.
- Regiões conexas do mapa (`src/core/map/components.ts`): use `rectReachable` antes de mandar unidades a um alvo e `wouldSeal` antes de a IA construir; o movimento físico usa `canStep` (sem cortar cantos).
- Ao mudar balanceamento, rode `npm run balance` e observe minutos das idades (Clássica ~5, Heroica ~15-20, Mítica ~20-28).
- O que o jogador guarda (save, replay, campanha, conquistas, opções, mapas) se grava com `storeSet`/`storeRemove` de `src/game/cloud.ts`, nunca `localStorage.setItem` direto: no Electron isso espelha em `userData/saves/*.json` (Steam Cloud). Chave nova que deve ir para a nuvem entra em `CLOUD_FIXED_KEYS` **e** em `desktop/cloud.cjs` (o teste compara as duas e acusa gravação direta).
- Commits em português, com o rodapé de atribuição exigido pela sessão.

## Memória do projeto (ler primeiro em toda sessão)
- **Plano completo e cronograma**: `docs/ROADMAP.md` (fases 0–7, passos numerados, responsáveis V/A/T, marcos M1–M6, custos, riscos). É o documento vivo: ao concluir um passo, marque-o lá.
- **Roteiro da campanha**: `docs/STORY.md` (Titanomaquia, 12 missões, fichas com JSON, lacunas do motor, ordem de produção e testes por missão).
- **Design e arquitetura**: `docs/DESIGN.md` · **Publicação na Steam**: `docs/STEAM.md` e `desktop/README.md`.
- **Áudio**: sintetizado pelo próprio código (`src/audio/`: efeitos posicionais a partir dos efeitos/eventos do estado, ambiente por bioma, música generativa em modos gregos, volumes separados). **Controle**: `src/ui/gamepad.ts` (Xbox/Steam Deck, cursor virtual, menus navegáveis).
- **Visual**: o dono pediu **realismo** no visual (set/2026). Direção de arte e pipeline em `docs/ART.md`; Etapas 0, 1 e 2 (bake + integração: `src/render/art/` ArtLibrary com atlas + procedural, `views/UnitView|BuildingView`, `props.ts`; opção "Arte assada (beta)") concluídas — hoplita, cidadão, árvores e nós são sprites assados; Etapa 3 concluída (`docs/ART.md` Apêndice D): os **21 edifícios** assados em obra 0–2/pronto/dano 1–2/escombros com máscara de time, sombra, fumaça, fantasma e ícone no HUD (muralha ×16 por bitmask + retas com estandarte, portão com casa de guarda, torre por bitmask só na sombra, Centro Cívico por Idade, fazenda com 3 plantações, portal dos titãs com vórtice animado; estilos em `scripts/bake/page/buildings.js` + `page/rigs/buildings-economy.js` + `page/buildings-military.js`; pick dos edifícios pelo alfa do atlas em `src/render/art/alphaMask.ts`, edifício inimigo sob a névoa na última versão vista, opção "contorno de time" implementada); capturas de cidade por Idade (`docs/art/etapa3-cidade-*.png`, `scripts/artages.mjs`) aguardando o dono; Etapa 4 concluída (`docs/ART.md` Apêndice E): as **19 unidades não míticas** assadas (cidadão, hoplita + 17 da etapa: milícia, hipaspista, mirmidão, toxota, peltasta, arqueiro cretense, batedor, hipeu, hetairo, petróbolo, helépole, rei e os 5 heróis) — kit do rig humano escolhido só pelo manifesto (`scripts/bake/page/rigs/human.js`: armaduras, elmos, escudos, armas, capas, coroa/pele do leão/pétaso, cetro/velo, estatura), poses por arma e por herói em `art/poses/human.json` (Odisseu com parada própria `idle_odysseus`), rigs de cavalo + cavaleiro (porte, xairel, peitoral, testeira) e de cerco, registro `rigs/units.js`, `UnitView` genérico com `aim`/`run`/`ability` (habilidade Q lida da recarga do núcleo; andar avança pela distância com a passada medida no rig por `scripts/bake/measure.mjs`, barra de vida pelo topo do corpo por direção, pick pelo alfa, cadáver 8 s) e carregamento de unidades POR TIPO (página servida só depois de subir para a GPU); orçamento de VRAM: sombra a ½ resolução nos atlas (`SHADOW_TEXEL`) e páginas em múltiplos de 32 px (`PAGE_ALIGN`), `art:check` com teto por escala; míticas e titãs seguem procedurais (Etapa 6); toda entrega visual vem com captura antes/depois (`node scripts/actionshot.mjs http://localhost:4173/ saida.png`) e é olhada com a ferramenta Read.
- **Estado atual**: Fase 0 concluída (fatia vertical: skirmish, campanha com 3 missões, Horda, multiplayer lockstep, replays, Electron). Fase 1 (jogabilidade sólida) quase concluída: guarnição, portões, formações, IA "Muito difícil", i18n PT/EN, opções (tela cheia, escala, qualidade), atalhos e caça a bugs (economia corrigida). Caça a bugs (7 lentes, 48 correções) concluída. Pendente: balanceamento contínuo (`npm run balance`) com base nos playtests humanos. Feitos: lobby (chat, ping, kick, atraso dinâmico), reconexão por instantâneo, modos de jogo e tipos de mapa (5.1). Fase 3.2/3.3 (editor de cenários e mapas fixos): design fechado em `docs/EDITOR.md` (formato, validação, plano em 5 etapas); Etapa 0 (UI de mapa fixo no skirmish e no lobby) concluída; Etapas 1–2 (núcleo: validateMap, mapHash, entidades pré-colocadas, kit inicial, hash do terreno; biblioteca de mapas, seletor no skirmish/lobby, mapa embutido) concluídas; Etapa 3 (editor MVP: `src/editor/` ops/MapEditor/panel, aba Editor no menu, `startEditor/testFromEditor` em `main.ts`, `scripts/playtest-editor.mjs`) concluída; Etapa 4 concluída (Corrigir para todos os avisos corrigíveis, tabela de recursos por início, balde, conta-gotas, redimensionar desfazível, atalhos finais sem A/R/U — Unidades = `M`, regiões = `L`, conta-gotas = `P`; `src/core/map/check.ts` em `map:check` e nos testes; mapas oficiais Estreito e Egeu redesenhados por `scripts/maps/*.ts`); Etapa 5 (cenários em JSON) concluída. Fase 3.2/3.3 fechadas. Campanha (3.4/3.5): motor G0–G13 e G17 (G4 HUD relativo, G6 `remove`/guarnição/`maxAge`/`forbid` em `src/core/sim/restrictions.ts`, G8 nomes, G9 vida de chefe, G10 relíquias fixas, G11 poderes por roteiro e G13 autoria de abate com registro em `src/core/scenario/log.ts`, G12 `ability`, G17 contador de repeat), marionetes explícitas (`puppet: true`), resultado por time e harness estrito (`MISSION_SCRIPTS`: roteiro vence dentro de `expect` nas 3 dificuldades); falas em fila no HUD (`src/ui/dialogue.ts`); missões produzidas: m1–m4 (Ato I; m4 com o 1º mapa fixo da campanha, `scripts/maps/m4_caucaso.ts`) , m5–m8 (Ato II completo; m5 e m8 com mapas fixos; m8 fecha o conteúdo do EA com o chefe-Titã Oceano em três marés; harness com `reserve` em lista, `detach`, `keepPowers`, `variants` e `atEnd`) e m9–m12 (Ato III: m9 jogando como Hades com relíquias fixas; m10 cerco + rei da colina; m11 êxodo com o arconte; m12 batalha final com Cronos em fases); **campanha completa: 12 missões em 3 atos, 6 mapas fixos gerados por `scripts/maps/*.ts`**, roteiro estrito vencendo nas 3 dificuldades (`npx tsx scripts/missions.ts`, ~12 min); pendente: playtests humanos e ajuste fino de dificuldade. Build Linux do Electron verificada (`app://`, `playtest-desktop.mjs`). Viés de posição do motor/IA corrigido (26/09/2026, em duas rodadas: referenciais relativos ao centro do mapa, desempates locais, rodízio das IAs por rodada, a IA não fecha o acesso de recursos, `SIM_VERSION` no relay; `docs/EDITOR.md`, "Justiça de posição"). Roteiros reajustados (só `testing.ts`): m2, m5 (a Liga ataca aos ~7 min no Difícil) e m7 (reagrupar antes do assalto); a janela da m4 foi para 15m–39m (a missão ficou mais fácil no Fácil; `docs/STORY.md` §4).
- **Rotina combinada com o dono do projeto**: ele joga 2–3 partidas por semana e manda uma lista curta de problemas; o agente entrega correções + uma feature com testes e balanceamento automático; a cada duas semanas o roteiro é revisado.
- **Decisões tomadas**: TypeScript + PixiJS + Electron (não Unity/Godot) para que o agente construa e verifique tudo sozinho; simulação determinística separada da renderização para multiplayer/replays; arte procedural como placeholder até a Fase 2; interface em PT-BR primeiro, EN na Fase 1.7.
- **Fase 6 (parte do agente, 26/09/2026)**: conquistas prontas para o Steamworks (6.4), Steam Cloud por espelho em arquivos (6.4; `docs/STEAM.md` §4), tela Créditos + licenças geradas (6.7), `docs/LEGAL.md` revisado contra o código (6.7) e matriz/checklist de lançamento em `docs/QA.md` (6.8). O resto da Fase 6 depende do dono (`docs/LEGAL.md` §7, checklist manual de `docs/QA.md`).
- **Pendências que dependem do dono**: horas semanais disponíveis, orçamento de arte, conta Steamworks/empresa (Fase 6), campos legais (CNPJ, contato, foro), operador do relay em produção.
